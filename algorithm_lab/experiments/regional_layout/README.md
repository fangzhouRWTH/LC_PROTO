# Regional Layout Experiments

Use this folder for standalone layout algorithms that generate static asset footprints or asset import plans from placeholder regions such as plazas, sidewalks, roadsides, or courtyards.

Create one subfolder per experiment and include a short `README.md`, `run.py`, `input.example.json`, and `output.example.json` when possible.
